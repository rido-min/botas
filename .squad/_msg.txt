Fix Python 06-state-bot sample: actually send replies (#361)

The Python sample handler built a reply in temp state but forgot to call
ctx.send(). Added send calls in all three code paths (regular message,
"reset", "whoami").

Also added an offline mode: when CLIENT_ID is unset, replies are logged
to the console with [OFFLINE] prefix instead of being sent to Bot
Service. This lets users kick the tires without provisioning a bot.

Sample-only change — no library impact. .NET and Node samples already
send replies correctly; mirroring the offline-mode UX is left as an
optional follow-up.

Tests: 216 Python tests pass, ruff clean.

Reported by: rido

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
