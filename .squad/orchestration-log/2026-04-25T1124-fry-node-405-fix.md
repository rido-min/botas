# Agent Spawn Log: Fry (Node.js GET 405 Fix)

**Date:** 2026-04-25T11:24:31Z  
**Agent:** Fry (Node Dev)  
**Issue:** #250  
**Branch:** fix/node-get-405  
**PR:** #255  
**Status:** Complete  

## What Was Done

Fixed Node.js (Express adapter) returning 404 for GET `/api/messages`. Added `app.all()` catch-all immediately after `app.post()` registration to return 405 MethodNotAllowed with `Allow: POST` header, matching .NET and Python behavior.

## Outcome

Cross-language parity restored for non-POST method handling. Decision documented in `.squad/decisions/inbox/fry-node-405.md`.

## Decision Summary

- Pattern: register `app.all()` after specific method handlers to catch unsupported methods
- Apply same pattern if new adapters (Hono, Fastify) are added
- Standard error response format deferred to Issue #247

## Files Modified

- `node/packages/botas-express/src/bot-app.ts` — added `app.all()` handler

## Next Steps

1. Error response format for 405 to be implemented under Issue #247 once standard decided
2. Monitor Hono/Fastify adapters if added
