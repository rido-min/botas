# Fry — Node.js Counter Handlers

**Timestamp:** 2026-05-21T22:30:38Z

## Task
Added counter + reset handlers to `node/samples/test-bot/index.ts` with MemoryStorage user scope.

## Result
✅ **203 Node tests pass** (no regressions).

Counter behavior:
- "counter" → "Count: N" (increments per user, starts at 1)
- "reset" → "Counter reset"
- Stored in user scope via MemoryStorage middleware

All other test-bot commands (echo, card, submit, mention) preserved.

## Changes
- `node/samples/test-bot/index.ts` — added counter + reset handler registration
