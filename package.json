{
  "name": "botas",
  "version": "0.0.3",
  "description": "bot as samples",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build --verbose tsconfig.build.json"
  },
  "author": "rido",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "@types/node": "^20.6.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  }
}
