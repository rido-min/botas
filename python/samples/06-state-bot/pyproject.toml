[project]
name = "botas-sample-state-bot"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "botas-fastapi",
]

[tool.uv.sources]
botas = { path = "../../packages/botas", editable = true }
botas-fastapi = { path = "../../packages/botas-fastapi", editable = true }
